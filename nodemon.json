{
  "watch": ["src"],
  "ext": "ts,json",
  "exec": "NODE_ENV=dev node --experimental-specifier-resolution=node --inspect --loader ts-node/esm ./src/app.ts",
  "delay": 5,
  "signal": "SIGTERM",
  "inspect": "true"
}
