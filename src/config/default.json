{
  "fastify": {
    "logger": true,
    "port": 8080,
    "host": "localhost"
  },
  "logger": {
    "level": "info"
  },
  "storage": {
    "areas": [
      {
        "name": "default",
        "path": "/tmp/default",
        "description": "Default area"
      }
    ]
  }
}